# Termux-safe requirements for Andyria
# cryptography and psutil are installed via `pkg install` (pre-built binaries)
# uvicorn runs without [standard] extras to avoid uvloop / httptools C-build failures

fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
typer>=0.12.0
rich>=13.0.0
aiofiles>=23.0.0
pyyaml>=6.0
